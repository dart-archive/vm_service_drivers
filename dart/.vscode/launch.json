{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Dart command line",
      "type": "dart-cli",
      "request": "launch",
      "cwd": "${workspaceRoot}",
      "sdkPath": "${command.sdkPath}",
      "program": "${workspaceRoot}/tool/generate.dart",
      "args": []
    }
  ]
}